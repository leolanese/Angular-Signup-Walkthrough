import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'myNewApp',
  webDir: 'dist/my-new-app',
  bundledWebRuntime: false
};

export default config;
